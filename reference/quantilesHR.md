# Quantiles of hazard ratios

Quantiles of hazard ratios

## Usage

``` r
quantilesHR(subFrame, probs = c())
```

## Arguments

- subFrame:

  A vibration data frame subset.

- probs:

  Probability quantiles.

## Value

A data frame with columns `probs` and `HR`.
