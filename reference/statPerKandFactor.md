# Summary statistics per k and factor level

Summary statistics per k and factor level

## Usage

``` r
statPerKandFactor(vibFrame)
```

## Arguments

- vibFrame:

  A vibration data frame.

## Value

A data frame with columns `k`, `estimate`, `pvalue`, and `factor_level`.
