# Summary statistics per k

Computes the median estimate and median p-value for each value of k
(number of adjustors).

## Usage

``` r
statPerK(vibFrame)
```

## Arguments

- vibFrame:

  A vibration data frame.

## Value

A data frame with columns `k`, `estimate`, and `pvalue`.
