# Median estimate and p-value

Median estimate and p-value

## Usage

``` r
meanEstimate(subFrame)
```

## Arguments

- subFrame:

  A vibration data frame subset.

## Value

A one-row data frame with `estimate` and `pvalue`.
