# Mean Manhattan distance

Computes pairwise Manhattan distances, normalizes by absolute values,
and returns the mean as a percentage.

## Usage

``` r
mean_manhattan(arr)
```

## Arguments

- arr:

  A numeric vector.

## Value

Mean relative Manhattan distance (percent).
