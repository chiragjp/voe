# Quantiles of p-values

Quantiles of p-values

## Usage

``` r
quantilesPvalue(subFrame, probs = c(0.01, 0.25, 0.5, 0.75, 0.99))
```

## Arguments

- subFrame:

  A vibration data frame subset.

- probs:

  Probability quantiles.

## Value

A data frame with columns `probs` and `pvalue`.
